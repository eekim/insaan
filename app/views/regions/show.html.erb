<% content_for :sectionhead do %>
  <h2 class="entname"><%= @region.country.name %> - <%= @region.name %></h2>
<% end %>
<% content_for :tabnav do %>
  <ul class="tabnav">
    <li class="tabselected"><%= link_to 'People', '#' %></li>
  </ul>
<% end %>

<div class="results"><%# will_paginate @people %></div>
<div class="table-border">
  <table class="standard" cellpadding="0" cellspacing="0">
    <tr>
      <th width="50">&nbsp;</th>
      <th>member</th>
      <th>current job</th>
      <th>last event</th>
      <th>fellowship(s)</th>
    </tr>

    <%= render :partial => 'layouts/person', :collection => @people %>

  </table>
</div>
<div class="results"><%# will_paginate @people %></div>
